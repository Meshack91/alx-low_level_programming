#include "main.h"
/**
 * mul - multiplication of two integers
 * @a: the first number to multiply
 * @b: the second number
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
